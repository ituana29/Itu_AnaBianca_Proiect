@page
@model Itu_AnaBianca_Proiect.Pages.Categories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Clienti</h1>

<p>
    <a asp-page="Create">Creaza</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category[0].CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Category) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoryName)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Editeaza</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Detaii</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Sterge</a>
                </td>
            </tr>
        }
    </tbody>

    <img src="C:\Users\sipie\Desktop\Itu_AnaBianca_Proiect\Images\imagine2.jpg" style="width: 500px; height: 300px;"/>
</table>
